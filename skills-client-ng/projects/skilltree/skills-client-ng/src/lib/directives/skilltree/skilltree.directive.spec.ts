import { SkilltreeDirective } from './skilltree.directive';

describe('SkilltreeDirective', () => {
  it('should create an instance', () => {
    const directive = new SkilltreeDirective();
    expect(directive).toBeTruthy();
  });
});
